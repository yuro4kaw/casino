import React from "react";
import "@/styles/main.scss";
import "@/styles/_container.scss";

const Header = () => {
  return (
    <header className="container">
      <span className="logo">MonoCasino</span>
    </header>
  );
};

export default Header;
